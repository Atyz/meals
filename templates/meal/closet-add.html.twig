{% extends 'base.html.twig' %}

{% block title %}Ajouter ce plat{% endblock %}

{% block body %}
    <h1 data-tf="meal.edit">
        Ajouter ce plat
    </h1>

    {{ form_start(form, {'attr': {
        'novalidate': 'novalidate',
        'data-tf': 'meal.form'
    }}) }}
        {% include 'meal/form.html.twig' %}

        <button type="submit" class="btn btn-primary">
            Ajouter ce plat
        </button>

        <a href="{{ url('meal_closet') }}" class="btn btn-secondary">
            Retour
        </a>
    {{ form_end(form) }}
{% endblock %}
